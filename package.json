{
  "name": "lockdown-life-3d",
  "private": true,
  "version": "3.6.0-factions",
  "type": "module",
  "description": "Lockdown Life 3D - a gritty 3D isometric prison-life sandbox simulator",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit && vite build"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
}
